import dom from "./dom";
import array from "./array";
import cookie from "./cookie";
import location from "./location";
import object from "./object";
import string from "./string";
import animate from "./animate";

export default {
    dom : dom,
    cookie : cookie,
    location : location,
    array : array,
    string : string,
    object : object,
    animate : animate
};

export { dom, array, cookie, location, object, string, animate };